// @ts-check
import { defineConfig } from "astro/config";

// This site is deployed as a GitHub Pages *project* page at
// https://dipamsen.github.io/archive/  (repo name: "archive")
export default defineConfig({
  site: "https://dipamsen.github.io",
  base: "/archive",
});
